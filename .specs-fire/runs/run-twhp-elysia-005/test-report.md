---
run: run-twhp-elysia-005
work_item: verdict-save-terminal-score
intent: score-change-finality
generated: 2026-08-24T04:20:00Z
status: passing
---

# Test Report: Score change saves as a terminal verdict

## Summary

| Category | Passed | Failed | Skipped |
|----------|--------|--------|---------|
| Full suite (`bun test`) | 518 | 0 | 1 |
| This work item (`evaluator-review.save.integration.test.ts`) | 26 | 0 | 0 |

The save suite grew 19 → 26 tests. No test was deleted; the two that encoded the old mapping
were corrected in place.

## Acceptance Criteria Validation

- ✅ **`change_score` → `recommended` with `verdictChoice` + `description` retained** — asserted, plus the persisted log row.
- ✅ **`reject` → `rejected` + null `verdictChoice`** — asserted.
- ✅ **`approve` unchanged** — pre-existing tests untouched and passing.
- ✅ **No-op `change_score` still 400** — pre-existing test passing.
- ✅ **Evidence guard refuses an unsupported target choice** — 400 naming `file_3_1`; the answer stays `in_review`, proving the guard precedes the insert.
- ✅ **Guard passes when files support the choice** — downgrade to `"1"` → 200.
- ✅ **`special === 3` single-file rule** — downgrade to `"2"` with only `file_3_1` → 400 `choice 2 requires file_2_1`.
- ✅ **Standard-question exemption** — q23 with no `file_3_1` accepts `"3"` → 200.
- ✅ **Hard reject never evidence-checked** — `reject` on a file-less choice → 200.
- ✅ **`recommended` edit guard governs a score change** — non-author tier-1 403, author 200.
- ✅ **Zero side effects** — `coverLogs` count unchanged across a save.
- ✅ **`bun test` passes** — 518/0.

## Tests Written

### Integration Tests

- `src/service/evaluator-review.save.integration.test.ts` — new `Evidence guard on change_score`
  describe (6 tests) plus an authorship test for settled score changes; fixture now seeds
  `fileUrl1_1`/`fileUrl2_1` on every Answer, adds a `special === 3` Answer (q14) holding
  `file_3_1` alone, and documents that the Safety Answer (q23) is standard-backed.

## Test Commands

```bash
bun test
bun test src/service/evaluator-review.save.integration.test.ts
```

## Issues Found

| Issue | Severity | Status |
|-------|----------|--------|
| `bun run check` (biome `--write`) reformats two unrelated files — `src/schema/authentication.ts`, `src/service/score.test.ts` | low | Reverted; pre-existing repo drift, not caused by this run |
| 32 pre-existing `tsc --noEmit` errors, incl. `evaluator-review.pastyear.test.ts` calling the verdict body with `outcome:` instead of `decision:` | low | Pre-existing; count unchanged by this run (32 before, 32 after) |
| Finalize-side tests still build `rejected` + `verdictChoice` rows directly, so they exercise the legacy shape rather than the new one | info | Intentional — that is exactly the legacy compatibility case `finalize-settles-score` must handle; those tests become its coverage |
| The `yaml` package needed by FIRE's run scripts is absent from the project | low | Installed into the session scratchpad and used via `NODE_PATH`; `package.json` untouched, per the ask-before-installing rule |

## Ready for Completion

- [x] All tests passing
- [x] All acceptance criteria validated
- [x] No critical issues open

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-twhp-elysia-005*
