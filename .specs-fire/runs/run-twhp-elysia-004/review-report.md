---
run: run-twhp-elysia-004
work_item: factory-grace-window
intent: fiscal-year-addressing
generated: 2026-08-22T17:05:00Z
status: passed
---

# Code Review: run-twhp-elysia-004

## Work Item: factory-grace-window

### Files Reviewed

| File | Action |
|------|--------|
| `src/utils.ts` | Modified — `GRACE_DAYS`, `factoryGraceApplies`, surfaced on `utilities()` |
| `src/service/answer.ts` | Modified — `resolveWritableYear` and four threaded write paths |
| `src/routes/factories/assessments/index.ts` | Modified — `FiscalYearQuery` on four write endpoints |
| `src/service/factory-grace.test.ts` | Created |

### Auto-Fixed

| Issue | Fix |
|-------|-----|
| Formatting and import ordering | `biome check --write`, scoped per changed file |

### Reviewed — No Action

| Category | Finding |
|----------|---------|
| Security | `factoryId` still comes from the JWT subject on all four paths. The year parameter selects among the caller's **own** Covers and cannot reach another Factory's data |
| Security | Enrollment paths are untouched and expose no `fiscalYear`, so closed-year enrollment stays immutable to its owner by construction rather than by check |
| Ordering | The grace check precedes the Cover lookup, because it determines which window the lookup uses. This inverts run 003 deliberately, and the reason is recorded in the code comment so it does not read as an inconsistency |
| Single declaration | One `GRACE_DAYS`, one `factoryGraceApplies`, one `resolveWritableYear`. A window duplicated across four paths would let a Factory be admitted by one and refused by the next — losing work at the last step |
| Recurrence | The window is expressed relative to `fiscalYearBoundary`, never as 2026 literals, and three tests assert it in other fiscal years |
| Restraint | No guard was added to `saveAnswer`. The design's investigation showed the path is unreachable; a defensive check there would be dead code that looks load-bearing |
| Standards | `status(code, body)` returns throughout; no throws. File I/O ordering on the submission path is untouched |

### Design Claims Verified

The design made two claims that implementation could have falsified:

1. **"Grace widens selection rather than gating."** Confirmed — the four paths take no `coverId`, so
   the year had to be resolved before the lookup. A gate was structurally impossible.
2. **"Placing the policy in `src/schema/fiscal-year.ts` may be circular."** Confirmed —
   `src/utils.ts:5` already imports from it. The plan named the fallback in advance, so this was a
   planned branch rather than a deviation.

### Pre-Existing — Not Touched

| Finding | Location |
|---------|----------|
| Host-local date construction in a test | `factory-pagination.integration.test.ts:159-160` |
| Left/inner join asymmetry under `enrolled=false` | `docs/adr/0008:56` |
| 36 Biome findings | Baseline, 12 files, mostly tests |

### Documentation

None yet, by plan. The complete Factory write rule now exists, but `concurrent-years-and-audit` — the
second item in this run — still adds the audit trail. `docs/api-conventions.md` is written once, at
the end of this run, describing the whole rule rather than successive halves.

### Lint Verification

```
before:  3 errors · 30 warnings · 3 infos
after:   3 errors · 30 warnings · 3 infos
```

### Outcome

**Passed.** Three source files changed, zero regressions across 500 tests, both design claims
verified, one guard deliberately not added.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-twhp-elysia-004*

---

## Work Item: concurrent-years-and-audit

### Files Reviewed

1 test file created, 4 documentation files updated. **No production code changed.**

### Reviewed — No Action

| Category | Finding |
|----------|---------|
| Scope | This item changed no production code. Its value is verification of a condition only now constructible, plus the documentation deferred twice on purpose |
| Test design | Every `.limit(1)` self-read is asserted individually rather than reasoned about. These were written under an assumption grace has now broken, so inspection was not sufficient |
| Proof, not inference | `coverService.create` is asserted against a live database. The work item explicitly said "confirm by test; do not assume", and reading the `enroll_id` duplicate check would have been assuming |
| Negative assertion | Expiry is verified by what does **not** happen — an invariant log count. Asserting absence is weaker than asserting presence, so it is paired with a positive check that the status is still `in_progress` |
| Honest reporting | The attribution criterion is marked **not delivered**, with both blocked routes named. A `customProps` change was considered and rejected because it would have looked like attribution without being it |

### Documentation Review

| File | Verified |
|------|----------|
| `docs/api-conventions.md` | Three new sections: write authority, the grace window with its endpoint inclusions **and exclusions**, and expiry. The exclusions matter as much as the inclusions — they are what bound the rule |
| `docs/business-rules.md` | BR-06a (write authority) and BR-06b (expiry) added. BR-06a carries the attribution limitation. BR-07 gains a note stating explicitly what this intent did **not** change |
| `docs/handover.md` | Write rule in the critical-rules list |
| `.specs-fire/standards/api-conventions.md` | Mirrors the rule, including the known limitation |

No statement in any of the four contradicts shipped behaviour, and each records a limitation rather
than implying completeness.

### Outcome

**Passed.** Zero production changes, zero regressions across 512 tests, one criterion honestly
reported as not delivered.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-twhp-elysia-004*
